# Core (config/settings.py — INSTALLED_APPS: tailwind, theme)
# Django stack: conda env web_service_env
Django==6.0.5
django-tailwind==4.4.2

# accounts.models — ImageField for profile_picture
# conda env base (active)
Pillow==12.1.1

# products/data/raw/data_crawling/selenium_auto_module.py
# selenium / webdriver-manager: not listed in conda (install via pip)
selenium==4.44.0
webdriver-manager==4.0.2
beautifulsoup4==4.14.3
